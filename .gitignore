# compiled binary
/rs

# local go workspace (machine-specific relative path to ../alcatraz)
go.work
go.work.sum

# generated reports
risk-report.html
risk-report.json
*.png

# local incremental scan state
.risk-analyzer/

# prebuilt binaries injected into the npm platform packages by npm/build.mjs
/npm/rs-*/bin/
