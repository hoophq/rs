# compiled binary
/rs
/hooprs

# generated reports
risk-report.html
risk-report.json
#*.png

# local incremental scan state
.risk-analyzer/

# local multi-module dev against a sibling alcatraz checkout
/go.work
/go.work.sum

# prebuilt binaries injected into the npm platform packages by npm/build.mjs
/npm/rs-*/bin/

# brew archives, checksums, and rendered formula produced by homebrew/build.mjs
/homebrew/dist/
